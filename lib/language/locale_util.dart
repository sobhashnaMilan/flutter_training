import '../../util/enum_all/enums_all.dart';
import 'package:flutter/material.dart';

class LocaleUtil {
  static Locale englishLocale = Locale(LanguageCodeEnum.en.name, 'US');

  static Locale germanyLocale = Locale(LanguageCodeEnum.de.name, 'DE');

  static Locale arabLocale = Locale(LanguageCodeEnum.ar.name, 'AR');

  static Locale hindiLocale = Locale(LanguageCodeEnum.hi.name, 'HI');
}
